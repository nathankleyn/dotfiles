#!/bin/sh

#
# ~/.xinitrc
#
# Executed by startx (run your window manager from here)
#

start-pulseaudio-x11
xrdb -merge ~/.Xresources
feh --bg-fill ~/photos/the-witness-1.png
i3 -V > /tmp/i3log 2>&1
